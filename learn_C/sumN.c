#include <stdio.h>

int main(void)
{
	int i;
	int j;
	int sum = 0;

	printf("Enter a set of number: ");
	scanf("%d", &i);

	for (j = 1i; j <= i; j++)
	{
		sum = sum + j;
	}
	printf("sum is: %d", sum);
	printf("\n");
	return (0);
}
